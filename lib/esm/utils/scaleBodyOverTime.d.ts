import { MatterBody } from '../objects/MatterBody';
import { XYPos } from '../matter_types';
export declare function scaleBodyOverTime(theBody: MatterBody, onX: number, onY: number, point: XYPos, deltaT: number): void;
